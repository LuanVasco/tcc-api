// import { config } from 'dotenv';
// config();

// import WhatsApp from 'whatsapp';

// const senderNumberId = Number(process.env.WHATSAPP_SENDER_NUMBER_ID);
// const accessToken = process.env.WHATSAPP_ACCESS_TOKEN;

// const waClient = new WhatsApp(senderNumberId);
// waClient.updateAccessToken(accessToken);
// waClient.updateSenderNumberId(senderNumberId);

// export default waClient;
